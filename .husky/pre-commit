#!/bin/sh

# Get list of staged TypeScript/JavaScript files
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx|js|jsx)$')

# Export mocks documentation
echo "📝 Exporting mock API documentation..."
yarn export-mocks

# Check if mock docs changed
if git diff --quiet docs/mocks/; then
  echo "✅ Mock docs are up to date"
else
  echo "📄 Mock docs updated, staging changes..."
  git add docs/mocks/
fi

# Verify API schemas are in sync with OpenAPI spec
echo ""
echo "🔍 Verifying API schemas..."
yarn verify-schemas
SCHEMA_EXIT_CODE=$?

if [ $SCHEMA_EXIT_CODE -eq 0 ]; then
  echo "✅ API schemas are in sync"
elif [ $SCHEMA_EXIT_CODE -eq 2 ]; then
  # Exit code 2 = API fetch failed, allow commit to proceed
  echo "⚠️  To fix schema mismatch when API is available, run: yarn generate-schemas"
else
  # Exit code 1 = Schema mismatch detected, block commit
  echo ""
  echo "❌ Schema mismatch detected! Commit blocked."
  echo "❌ Please run 'yarn generate-schemas' to fix the schemas before committing."
  exit 1
fi

# Run Prettier on staged files
if [ -n "$STAGED_FILES" ]; then
  echo ""
  echo "✨ Running Prettier..."
  echo "$STAGED_FILES" | xargs npx prettier --write

  # Re-stage formatted files
  echo "$STAGED_FILES" | xargs git add
  echo "✅ Code formatted"
fi

# Run ESLint on staged files
if [ -n "$STAGED_FILES" ]; then
  echo ""
  echo "🔍 Running ESLint..."
  if echo "$STAGED_FILES" | xargs npx eslint --no-warn-ignored; then
    echo "✅ Linting passed"
  else
    echo ""
    echo "❌ ESLint found errors. Please fix them before committing."
    exit 1
  fi
fi

# Run TypeScript type check
echo ""
echo "🔍 Running TypeScript type check..."
if yarn type-check; then
  echo "✅ Type check passed"
else
  echo ""
  echo "❌ Type check failed. Please fix TypeScript errors before committing."
  exit 1
fi

exit 0
