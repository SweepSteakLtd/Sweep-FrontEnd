import { Skeleton } from '~/components/Skeleton/Skeleton';

export const TournamentCardSkeleton = () => {
  return <Skeleton width="100%" height={160} borderRadius={12} style={{ marginBottom: 16 }} />;
};
